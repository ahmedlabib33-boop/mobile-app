# Project Intelligence Hub Mobile Shell

This folder contains the Flutter source for the Android/iOS WebView shell.

If `android/` or `ios/` are missing, run `flutter create --platforms=android,ios --project-name project_intelligence_hub --org com.samco.projectintelligence .` from this folder. The supplied build script does this automatically before building the Android APK.
